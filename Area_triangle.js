let base = prompt("Enter the Base length of the Triangle :");
let height = prompt("Enter the height of the Triangle :");

let area=(base*height)/2;

console.log("The Area of the Triangle is :"+area);

console.log(`The Area of the Triangle is :${area}`);